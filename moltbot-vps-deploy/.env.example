#/-------------------[DOTENV_PUBLIC_KEY]--------------------/
#/            public-key encryption for .env files          /
#/       [how it works](https://dotenvx.com/encryption)     /
#/----------------------------------------------------------/
# After encrypting, your DOTENV_PUBLIC_KEY_PROD will appear here
# DOTENV_PUBLIC_KEY_PROD="your-public-key-here"

# =============================================================================
# Moltbot Secrets (ONLY SECRETS - No Config!)
# =============================================================================
# Configuration choices go in config.json (version controlled)
# Only secrets go here (encrypted with dotenvx)
# 
# Setup:
# 1. Copy this file to .env.prod
# 2. Fill in your actual secret values
# 3. Encrypt: npx dotenvx encrypt -f .env.prod
# 4. Save .env.keys (DO NOT COMMIT)
# 5. Set DOTENV_PRIVATE_KEY_PROD in Dokploy
# =============================================================================

# -----------------------------------------------------------------------------
# GATEWAY
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
MOLTBOT_GATEWAY_TOKEN=your-secure-gateway-token-here

# -----------------------------------------------------------------------------
# LLM PROVIDER API KEYS (Optional - can use copied local config instead)
# -----------------------------------------------------------------------------
# NOTE: If you copy local config with copy-local-config.sh, you already have:
# - GitHub Copilot OAuth (from local setup)
# - Gemini via Antigravity (from local setup)
# These API keys below are only needed if setting up fresh without local config.

# Anthropic Claude
MOLTBOT_ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI
MOLTBOT_OPENAI_API_KEY=sk-...

# Google Gemini (direct API, not via Antigravity)
MOLTBOT_GEMINI_API_KEY=...

# GitHub Copilot (OAuth via local config preferred - see copy-local-config.sh)
MOLTBOT_GITHUB_COPILOT_TOKEN=...

# -----------------------------------------------------------------------------
# CHANNEL TOKENS
# -----------------------------------------------------------------------------

# Discord Bot Token
MOLTBOT_DISCORD_TOKEN=MTxxxxx.xxxxx.xxxxx

# Telegram Bot Token
MOLTBOT_TELEGRAM_TOKEN=123456789:ABCdefGHI...

# Slack Bot Token
MOLTBOT_SLACK_TOKEN=xoxb-...
MOLTBOT_SLACK_APP_TOKEN=xapp-...

# -----------------------------------------------------------------------------
# OPTIONAL INTEGRATIONS
# -----------------------------------------------------------------------------

# ElevenLabs (for voice synthesis)
MOLTBOT_ELEVENLABS_API_KEY=


